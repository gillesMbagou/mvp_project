# Généré depuis le patron patient-external-secret.yaml — mêmes clés
# (quarkus.datasource.username/password), chemin Vault propre au service.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: caresync-analytics-secrets
  namespace: caresync
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-caresync-analytics
    kind: ClusterSecretStore
  target:
    name: caresync-analytics-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_USER
      remoteRef:
        key: caresync/analytics
        property: quarkus.datasource.username
    - secretKey: DB_PASS
      remoteRef:
        key: caresync/analytics
        property: quarkus.datasource.password
