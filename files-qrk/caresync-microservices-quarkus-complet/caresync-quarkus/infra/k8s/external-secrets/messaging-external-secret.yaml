# Généré depuis le patron patient-external-secret.yaml — mêmes clés
# (quarkus.datasource.username/password), chemin Vault propre au service.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: caresync-messaging-secrets
  namespace: caresync
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-caresync-messaging
    kind: ClusterSecretStore
  target:
    name: caresync-messaging-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_USER
      remoteRef:
        key: caresync/messaging
        property: quarkus.datasource.username
    - secretKey: DB_PASS
      remoteRef:
        key: caresync/messaging
        property: quarkus.datasource.password
