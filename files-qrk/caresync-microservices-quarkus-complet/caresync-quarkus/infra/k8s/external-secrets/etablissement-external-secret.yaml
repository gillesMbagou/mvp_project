# Généré depuis le patron patient-external-secret.yaml — mêmes clés
# (quarkus.datasource.username/password), chemin Vault propre au service.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: caresync-etablissement-secrets
  namespace: caresync
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-caresync-etablissement
    kind: ClusterSecretStore
  target:
    name: caresync-etablissement-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_USER
      remoteRef:
        key: caresync/etablissement
        property: quarkus.datasource.username
    - secretKey: DB_PASS
      remoteRef:
        key: caresync/etablissement
        property: quarkus.datasource.password
