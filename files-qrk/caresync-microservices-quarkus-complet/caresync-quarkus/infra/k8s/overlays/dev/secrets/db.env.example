# Copier en db.env (jamais committé, cf. .gitignore) et remplir depuis les
# GitHub Secrets DEV_DB_USER / DEV_DB_PASSWORD (voir backend-cd-dev.yml) :
#   envsubst < secrets/db.env.example > secrets/db.env
DB_USER=${DEV_DB_USER}
DB_PASS=${DEV_DB_PASSWORD}
