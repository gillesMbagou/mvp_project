apiVersion: apps/v1
kind: Deployment
metadata:
  name: caresync-q-alert
  labels:
    app.kubernetes.io/name: caresync-q-alert
    app.kubernetes.io/part-of: caresync
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: caresync-q-alert
  template:
    metadata:
      labels:
        app.kubernetes.io/name: caresync-q-alert
        app.kubernetes.io/part-of: caresync
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /q/metrics
        prometheus.io/port: "8093"
    spec:
      serviceAccountName: caresync-alert
      containers:
        - name: app
          image: caresync-q-alert
          ports:
            - name: http
              containerPort: 8093
          envFrom:
            - configMapRef:
                name: caresync-common-config
            - secretRef:
                name: caresync-alert-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
