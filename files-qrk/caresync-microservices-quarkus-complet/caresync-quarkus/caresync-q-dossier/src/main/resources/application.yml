quarkus:
  application:
    name: caresync-dossier-svc
  http:
    port: 8096

  oidc:
    auth-server-url: ${KEYCLOAK_URL:http://localhost:8180/realms/caresync}
    client-id: caresync-dossier-svc

  datasource:
    db-kind: postgresql
    username: ${DB_USER:caresync}
    password: ${DB_PASS:caresync}
    jdbc:
      url: jdbc:postgresql://${DB_HOST:localhost}:5432/caresync_dossier
  hibernate-orm:
    database:
      generation: update

  vault:
    kv-secret-engine-version: 2
    kv-secret-engine-mount-path: secret
    secret-config-kv-path: caresync/dossier

kafka:
  bootstrap:
    servers: ${KAFKA_BROKERS:localhost:29092}

# REST clients vers patient-svc / prescription-svc : à renseigner
# (quarkus.rest-client."<Interface>".url=...) une fois les interfaces
# @RegisterRestClient créées côté code.

"%dev":
  quarkus:
    devservices:
      enabled: true
    vault:
      devservices:
        enabled: true

"%prod":
  quarkus:
    vault:
      url: ${VAULT_ADDR:https://vault.caresync.internal:8200}
      authentication:
        kubernetes:
          role: caresync-dossier-svc
          jwt-token-path: /var/run/secrets/kubernetes.io/serviceaccount/token
